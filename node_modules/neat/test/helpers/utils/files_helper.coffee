{resolve} = require 'path'

root = resolve __dirname, '../../../'
fixturesRoot = resolve root, 'test/fixtures'

global.fixturesPaths = -> [fixturesRoot]
global.inContext = (path) -> resolve root, 'foo', path
global.inFixtures = (path) -> resolve fixturesRoot, path
